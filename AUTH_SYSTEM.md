# 🔐 Система авторизации

## Как работает авторизация

### Текущая реализация

**Автоматическая авторизация через Telegram WebApp**

1. **При открытии приложения в Telegram:**
   - `TelegramProvider` автоматически получает данные пользователя из Telegram WebApp
   - Проверяет, авторизован ли пользователь в Supabase
   - Если не авторизован → автоматически создает/авторизует пользователя

2. **Процесс авторизации:**
   - Отправляет запрос на `/api/auth/telegram` с данными Telegram пользователя
   - API создает или находит пользователя в Supabase по `telegram_id`
   - Генерирует magic link для авторизации
   - Устанавливает сессию в браузере

3. **Создание пользователя:**
   - Если пользователь не существует → создается через Supabase Admin API
   - Email: `telegram_{telegramId}@mubarakway.app`
   - Профиль создается автоматически через триггер `handle_new_user`
   - `telegram_id` сохраняется в профиле

### Нужна ли форма авторизации?

**НЕТ** - форма авторизации не нужна, так как:
- Приложение работает внутри Telegram WebApp
- Авторизация происходит автоматически при открытии
- Пользователь не вводит логин/пароль - используется Telegram ID

### Когда нужна авторизация?

Авторизация нужна для:
- ✅ Загрузки аватара (`/profile`)
- ✅ Создания пожертвований (`/donate`)
- ✅ Создания кампаний (`/campaigns/new`)
- ✅ Просмотра истории транзакций (`/profile`)
- ✅ Управления подписками (`/subscription`)

### Проблемы и решения

**Проблема:** "You must be logged in to upload avatar"
- **Причина:** Пользователь не авторизован в Supabase
- **Решение:** Автоматическая авторизация через Telegram при открытии приложения

**Проблема:** Сессия истекла
- **Причина:** Сессия Supabase может истечь
- **Решение:** Автоматическая повторная авторизация при следующем открытии

## Переменные окружения

Для работы авторизации нужны:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key  # ⚠️ ВАЖНО для автоматической авторизации

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=YourBotUsername
```

## API Endpoints

### `POST /api/auth/telegram`

Автоматическая авторизация через Telegram WebApp.

**Request:**
```json
{
  "telegramId": 123456789,
  "firstName": "Иван",
  "lastName": "Иванов",
  "username": "ivan_ivanov"
}
```

**Response:**
```json
{
  "success": true,
  "userId": "uuid",
  "actionLink": "https://...",
  "accessToken": "token"
}
```

## Как это работает технически

1. **Telegram WebApp** предоставляет данные пользователя через `initDataUnsafe`
2. **TelegramProvider** получает эти данные и проверяет авторизацию
3. **API `/api/auth/telegram`** создает/находит пользователя через Admin API
4. **Magic link** генерируется для авторизации
5. **Сессия** устанавливается в браузере через `verifyOtp` или `setSession`

## Безопасность

- ✅ Используется Supabase Admin API только на сервере
- ✅ `SUPABASE_SERVICE_ROLE_KEY` хранится только в переменных окружения
- ✅ Telegram `initData` проверяется (можно добавить проверку подписи)
- ✅ RLS политики защищают данные пользователей

## Улучшения в будущем

- [ ] Добавить проверку подписи Telegram `initData`
- [ ] Добавить refresh token для автоматического обновления сессии
- [ ] Добавить обработку ошибок авторизации с понятными сообщениями
- [ ] Добавить возможность выхода из системы

