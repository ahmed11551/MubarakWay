# 📊 Статус API данных в приложении

## ✅ Что уже работает с реальными данными:

### 1. **Страница кампаний** (`/campaigns`)
- ✅ Использует `getCampaigns()` из Supabase
- ✅ Подключена к реальной базе данных
- ✅ Отображает активные, завершающиеся и завершённые кампании
- **Источник**: Supabase таблица `campaigns`

### 2. **Страница фондов** (`/funds`)
- ✅ Использует `getFunds()` с fallback на Supabase
- ✅ Сначала пытается получить с `bot.e-replika.ru`
- ✅ Если не получается → использует Supabase
- **Источник**: `bot.e-replika.ru` API или Supabase таблица `funds`

### 3. **API эндпоинты** (работают):
- ✅ **GET `/api/campaigns`** - возвращает кампании из API или Supabase
- ✅ **GET `/api/stats`** - возвращает статистику из API или Supabase
- ✅ **POST `/api/telegram/webhook`** - обрабатывает команды бота

### 4. **Главная страница** (`/`)
- ✅ Секция "Активные кампании" использует `/api/campaigns` ✅
- ⚠️ **Карусель "Срочные сборы"** использует моковые данные (строки 34-71)

## ⚠️ Что нужно исправить:

### 1. Карусель со срочными сборами на главной
- **Сейчас**: моковые данные `urgentCampaigns`
- **Нужно**: использовать реальные данные из API или Supabase
- **Файл**: `app/page.tsx` (строки 34-71)

### 2. Детальная страница кампании
- **Сейчас**: моковые данные
- **Нужно**: использовать `getCampaignById()` из `lib/actions/campaigns.ts`
- **Файл**: `app/campaigns/[id]/page.tsx`

### 3. Детальная страница фонда
- **Сейчас**: моковые данные
- **Нужно**: использовать `getFundById()` из `lib/actions/funds.ts`
- **Файл**: `app/funds/[id]/page.tsx`

## 🧪 Как проверить, что API работают:

### 1. Проверка API кампаний:
```powershell
# Локально
Invoke-RestMethod -Uri "http://localhost:3000/api/campaigns?status=active&limit=3"

# На продакшене
Invoke-RestMethod -Uri "https://sadaka-pass.vercel.app/api/campaigns?status=active&limit=3"
```

**Ожидаемый результат**: `{"campaigns": [...]}` или `{"campaigns": []}` если нет данных

### 2. Проверка API статистики:
```powershell
$headers = @{ Authorization = "Bearer test_token_123" }
Invoke-RestMethod -Uri "http://localhost:3000/api/stats" -Headers $headers
```

**Ожидаемый результат**: 
```json
{
  "total_collected": 0,
  "active_donors": 0,
  "active_campaigns": 0,
  "average_check": 0
}
```

### 3. Проверка в браузере:
- Откройте: http://localhost:3000/campaigns
- Должны отображаться кампании из БД (или пусто, если нет данных)
- Откройте: http://localhost:3000/funds
- Должны отображаться фонды из БД (или тестовые, если выполнили seed)

## 📝 Важно:

1. **Если данных нет в БД** - это нормально. API вернёт пустой массив `[]`
2. **Чтобы увидеть данные**:
   - Выполните `scripts/008_seed_initial_data.sql` для тестовых фондов
   - Создайте кампании через форму (после подключения создания)
   - Или добавьте данные напрямую в Supabase

3. **API работает в таком порядке**:
   - Сначала пытается получить с `bot.e-replika.ru`
   - Если не получается → использует Supabase
   - Если и Supabase пуст → возвращает пустой массив

## ✅ Вывод:

**API данные уже работают!** Просто:
- Если в БД нет данных → увидите пустые списки
- Если есть данные → увидите реальные кампании и фонды
- API эндпоинты возвращают данные из Supabase или внешнего API

---

**Последнее обновление**: 2025-01-15

