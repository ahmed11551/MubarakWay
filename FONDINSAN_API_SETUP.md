# 🔗 Интеграция с API фонда Инсан (fondinsan.ru)

## Настройка

### Переменные окружения

Добавьте в `.env.local`:

```env
# Fondinsan API
FONDINSAN_API_BASE_URL=https://fondinsan.ru/api/v1
FONDINSAN_ACCESS_TOKEN=0xRs6obpvPOx4lkGLYxepBOcMju
```

## Структура API

### Эндпоинт: GET `/api/v1/programs`

**URL**: `https://fondinsan.ru/api/v1/programs?access-token={token}`

**Ответ**:
```json
{
  "success": true,
  "status": 200,
  "data": [
    {
      "id": 47,
      "title": "Помощь Хифз-центру",
      "url": "https://fondinsan.ru/project/47",
      "short": "",
      "description": "<p>Описание программы...</p>",
      "created": "2025-08-05 09:08:46",
      "image": "https://fondinsan.ru/uploads/cache/Programs/Program47/1bc0623de3-2_400x400.png",
      "default_amount": 100
    }
  ]
}
```

## Преобразование данных

Программы из API фонда Инсан автоматически преобразуются в формат фондов:

- `id` → `fondinsan_{id}` (для уникальности)
- `title` → `name` и `name_ru`
- `description` → `description_ru` (HTML сохраняется)
- `short` → `description` (текст без HTML, обрезается до 500 символов)
- `image` → `logo_url` и `image_url`
- `url` → `website_url`
- `default_amount` → `default_amount`
- Категория определяется автоматически по ключевым словам в названии/описании

## Приоритет источников данных

При получении фондов система использует следующий приоритет:

1. **Fondinsan API** (https://fondinsan.ru/api/v1/programs)
2. **Bot API** (bot.e-replika.ru/api/funds)
3. **Supabase** (локальная база данных)

## Категории программ

Автоматическое определение категории по ключевым словам:

- `zakat` - "закят"
- `orphans` - "сирот", "опекун"
- `water` - "вода", "колодец"
- `mosque` - "мечеть", "уборка"
- `education` - "образование", "хифз"
- `healthcare` - "медицин"
- `emergency` - "экстрен"
- `general` - по умолчанию

## Использование

Функции автоматически используют Fondinsan API при наличии настроенных переменных окружения:

- `getFunds()` - получение списка фондов
- `getFundById()` - получение фонда по ID (поддерживает `fondinsan_{id}`)
- `/api/funds` - API эндпоинт

## Тестирование

Для тестирования интеграции:

1. Убедитесь, что переменные окружения настроены
2. Проверьте страницу `/funds` - должны отображаться программы из Fondinsan
3. Проверьте детальную страницу фонда `/funds/fondinsan_{id}`
4. Проверьте выбор фонда при создании пожертвования

