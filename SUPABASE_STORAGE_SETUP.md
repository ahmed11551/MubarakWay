# 📦 Настройка Supabase Storage для загрузки изображений

## Шаг 1: Создание Storage Bucket

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Storage** в боковом меню
4. Нажмите **"New bucket"**
5. Заполните форму:
   - **Name**: `images`
   - **Public bucket**: ✅ Включите (чтобы изображения были доступны публично)
   - **File size limit**: `5242880` (5MB)
   - **Allowed MIME types**: `image/jpeg,image/png,image/webp,image/gif`
6. Нажмите **"Create bucket"**

## Шаг 2: Настройка политик безопасности (RLS)

После создания bucket, нужно настроить политики доступа:

### Политика для загрузки (INSERT)
```sql
-- Разрешить авторизованным пользователям загружать изображения
CREATE POLICY "Users can upload images"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (bucket_id = 'images');
```

### Политика для чтения (SELECT)
```sql
-- Разрешить всем читать изображения (так как bucket публичный)
CREATE POLICY "Public can read images"
ON storage.objects FOR SELECT
TO public
USING (bucket_id = 'images');
```

### Политика для удаления (DELETE)
```sql
-- Разрешить пользователям удалять только свои изображения
CREATE POLICY "Users can delete own images"
ON storage.objects FOR DELETE
TO authenticated
USING (
  bucket_id = 'images' 
  AND (storage.foldername(name))[1] = auth.uid()::text
);
```

## Шаг 3: Проверка работы

После настройки:
1. Создайте тестовую кампанию с изображением
2. Проверьте, что изображение загружается в bucket `images`
3. Убедитесь, что изображение доступно по публичному URL

## Структура папок

Изображения сохраняются в следующей структуре:
```
images/
  campaigns/
    {user_id}/
      {timestamp}-{random}.jpg
```

## Примечания

- Максимальный размер файла: 5MB
- Поддерживаемые форматы: JPEG, PNG, WebP, GIF
- Изображения автоматически сжимаются на клиенте перед загрузкой (1920x1080, качество 0.8)
- Каждый пользователь может загружать только в свою папку (`{user_id}/`)

