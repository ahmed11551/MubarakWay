# 🔍 Проверка работы API данных

## ✅ Файл .env.local создан!

Теперь нужно запустить приложение и проверить API.

## 🚀 Шаг 1: Запустите приложение

```bash
pnpm dev
```

Дождитесь сообщения: `Ready - started server on 0.0.0.0:3000`

## 🧪 Шаг 2: Проверьте API в браузере

### 1. API Кампаний (без авторизации):
Откройте в браузере:
```
http://localhost:3000/api/campaigns?status=active&limit=3
```

**Ожидаемый результат:**
```json
{
  "campaigns": []
}
```
или
```json
{
  "campaigns": [
    {
      "id": "...",
      "title": "...",
      ...
    }
  ]
}
```

### 2. API Статистики (требует авторизацию):
Откройте в браузере (с авторизацией через расширение или Postman):
```
http://localhost:3000/api/stats
```

Или через PowerShell:
```powershell
$headers = @{ Authorization = "Bearer test_token_123" }
Invoke-RestMethod -Uri "http://localhost:3000/api/stats" -Headers $headers
```

**Ожидаемый результат:**
```json
{
  "total_collected": 0,
  "active_donors": 0,
  "active_campaigns": 0,
  "average_check": 0
}
```

## 📱 Шаг 3: Проверьте страницы

### 1. Страница кампаний:
```
http://localhost:3000/campaigns
```
- Должна загружаться без ошибок
- Если есть кампании в БД → покажет их
- Если нет → покажет "Нет активных кампаний"

### 2. Страница фондов:
```
http://localhost:3000/funds
```
- Должна загружаться без ошибок
- Если выполнили seed → покажет тестовые фонды
- Если нет → покажет пустой список

## ✅ Что должно работать:

1. ✅ **API эндпоинты** отвечают (даже если данных нет)
2. ✅ **Страницы** загружаются без ошибок
3. ✅ **Подключение к Supabase** работает (если .env.local настроен)

## ⚠️ Если данных нет:

Это нормально! API работает, просто в базе нет данных.

Чтобы добавить тестовые данные:
1. Выполните `scripts/008_seed_initial_data.sql` в Supabase SQL Editor
2. Это добавит тестовые фонды

## 🐛 Если есть ошибки:

1. **Проверьте .env.local** - файл должен быть в корне проекта
2. **Проверьте логи** в терминале, где запущен `pnpm dev`
3. **Проверьте подключение к Supabase** в консоли браузера (F12)

---

**Готово!** После запуска `pnpm dev` API должны работать. 🎉

