# 🔗 Технические требования для интеграции с фондом Закят

## 📋 Что нам нужно от фонда Закят для интеграции

### 1. API Endpoints (Эндпоинты)

#### 1.1 Получение списка всех программ/проектов
**Требуется:**
- **Метод**: `GET`
- **URL**: `https://[домен-фонда-закят]/api/[версия]/programs` или аналогичный
- **Аутентификация**: Access Token в query параметре или заголовке
- **Пример**: 
  ```
  GET https://zakiat-fund.ru/api/v1/programs?access-token=YOUR_TOKEN
  ```
  или
  ```
  GET https://zakiat-fund.ru/api/v1/programs
  Authorization: Bearer YOUR_TOKEN
  ```

**Ожидаемый формат ответа:**
```json
{
  "success": true,
  "status": 200,
  "data": [
    {
      "id": 1,
      "title": "Название программы",
      "url": "https://zakiat-fund.ru/program/1",
      "short": "Краткое описание",
      "description": "Полное описание программы (может быть HTML)",
      "created": "2025-01-15 10:30:00",
      "image": "https://zakiat-fund.ru/uploads/image.jpg",
      "default_amount": 500,
      "category": "zakat" // опционально
    }
  ]
}
```

#### 1.2 Получение одной программы по ID
**Требуется:**
- **Метод**: `GET`
- **URL**: `https://[домен-фонда-закят]/api/[версия]/program/by-id/{id}`
- **Аутентификация**: Access Token
- **Пример**: 
  ```
  GET https://zakiat-fund.ru/api/v1/program/by-id/1?access-token=YOUR_TOKEN
  ```

**Ожидаемый формат ответа:**
```json
{
  "success": true,
  "status": 200,
  "data": [
    {
      "id": 1,
      "title": "Название программы",
      "url": "https://zakiat-fund.ru/program/1",
      "short": "Краткое описание",
      "description": "Полное описание",
      "created": "2025-01-15 10:30:00",
      "image": "https://zakiat-fund.ru/uploads/image.jpg",
      "default_amount": 500
    }
  ]
}
```

### 2. Аутентификация

**Требуется:**
- **Access Token** или **API Key** для доступа к API
- **Метод передачи**: 
  - Query параметр: `?access-token=TOKEN`
  - ИЛИ заголовок: `Authorization: Bearer TOKEN`
  - ИЛИ заголовок: `X-API-Key: TOKEN`

**Примеры:**
```env
# Вариант 1: Query параметр
ZAKAT_API_BASE_URL=https://zakiat-fund.ru/api/v1
ZAKAT_ACCESS_TOKEN=your_access_token_here

# Вариант 2: Bearer Token
ZAKAT_API_BASE_URL=https://zakiat-fund.ru/api/v1
ZAKAT_BEARER_TOKEN=your_bearer_token_here

# Вариант 3: API Key
ZAKAT_API_BASE_URL=https://zakiat-fund.ru/api/v1
ZAKAT_API_KEY=your_api_key_here
```

### 3. Структура данных программы

**Обязательные поля:**
- `id` (number) - уникальный идентификатор программы
- `title` (string) - название программы
- `description` (string) - описание программы

**Желательные поля:**
- `short` (string) - краткое описание (для карточек)
- `url` (string) - ссылка на страницу программы на сайте фонда
- `image` (string) - URL изображения программы
- `created` (string) - дата создания в формате ISO или "YYYY-MM-DD HH:mm:ss"
- `default_amount` (number) - рекомендуемая сумма пожертвования
- `category` (string) - категория программы (например: "zakat", "education", "healthcare")

**Опциональные поля (для улучшения интеграции):**
- `total_raised` (number) - общая собранная сумма
- `donor_count` (number) - количество доноров
- `goal_amount` (number) - целевая сумма (если есть)
- `deadline` (string) - срок завершения программы (если есть)
- `status` (string) - статус программы ("active", "completed", "pending")

### 4. Формат ответа API

**Успешный ответ:**
```json
{
  "success": true,
  "status": 200,
  "data": [...]
}
```

**Ошибка:**
```json
{
  "success": false,
  "status": 400,
  "error": "Описание ошибки",
  "message": "Детальное сообщение об ошибке"
}
```

**HTTP статус коды:**
- `200` - успешный запрос
- `400` - неверный запрос
- `401` - не авторизован (неверный токен)
- `404` - программа не найдена
- `500` - ошибка сервера

### 5. Дополнительные требования

#### 5.1 Rate Limiting (Ограничение частоты запросов)
**Желательно знать:**
- Максимальное количество запросов в минуту/час
- Лимиты на IP адрес или токен

#### 5.2 CORS (Cross-Origin Resource Sharing)
**Требуется:**
- Разрешить запросы с домена нашего приложения
- Или предоставить прокси-сервер для запросов

#### 5.3 Кэширование
**Желательно:**
- Указать время жизни данных (TTL)
- Или заголовки Cache-Control в ответах

#### 5.4 Версионирование API
**Требуется:**
- Версия API (например: `/api/v1/`)
- План миграции при обновлении API

### 6. Технические детали для разработки

#### 6.1 Timeout
- Рекомендуемый timeout: **10 секунд** на запрос
- Retry логика: **3 попытки** с экспоненциальной задержкой

#### 6.2 Обработка ошибок
- Fallback на Supabase при недоступности API
- Логирование всех ошибок для отладки

#### 6.3 Преобразование данных
Программы фонда Закят будут преобразованы в формат фондов:
- `id` → `zakat_{id}` (для уникальности)
- `title` → `name` и `name_ru`
- `description` → `description_ru` (HTML сохраняется)
- `short` → `description` (текст без HTML)
- `image` → `logo_url` и `image_url`
- `url` → `website_url`
- `default_amount` → `default_amount`
- `category` → `category` (если предоставлено)

### 7. Приоритет интеграции

**Приоритет 1 (высокий):**
- ✅ Получение списка всех программ
- ✅ Получение одной программы по ID
- ✅ Access Token для аутентификации

**Приоритет 2 (средний):**
- ⚠️ Категории программ
- ⚠️ Статистика (total_raised, donor_count)
- ⚠️ Изображения программ

**Приоритет 3 (низкий):**
- ⚠️ Фильтрация по категориям через API
- ⚠️ Поиск программ через API
- ⚠️ Пагинация результатов

### 8. Пример запроса для тестирования

```bash
# Получение всех программ
curl -X GET "https://zakiat-fund.ru/api/v1/programs?access-token=YOUR_TOKEN" \
  -H "Content-Type: application/json"

# Получение программы по ID
curl -X GET "https://zakiat-fund.ru/api/v1/program/by-id/1?access-token=YOUR_TOKEN" \
  -H "Content-Type: application/json"
```

### 9. Контакты для уточнения

**Вопросы, которые нужно уточнить у фонда Закят:**
1. Базовый URL API
2. Метод аутентификации (токен, ключ, OAuth)
3. Формат Access Token
4. Структура ответа API
5. Наличие документации API (Swagger/OpenAPI)
6. Ограничения по частоте запросов
7. CORS политика
8. Версионирование API

---

## 📝 Чеклист для фонда Закят

- [ ] Предоставить базовый URL API
- [ ] Предоставить Access Token или API Key
- [ ] Указать метод аутентификации (query param / header)
- [ ] Предоставить примеры запросов и ответов
- [ ] Указать формат дат (ISO / "YYYY-MM-DD HH:mm:ss")
- [ ] Указать формат изображений (полный URL или относительный путь)
- [ ] Предоставить список доступных категорий (если есть)
- [ ] Указать ограничения по частоте запросов
- [ ] Настроить CORS для нашего домена
- [ ] Предоставить документацию API (если есть)

---

## 🔄 План интеграции (после получения данных)

1. Создать файл `lib/zakat-api.ts` (аналогично `lib/fondinsan-api.ts`)
2. Добавить функции:
   - `fetchZakatPrograms()` - получение всех программ
   - `fetchZakatProgramById(id)` - получение программы по ID
   - `transformZakatProgramToFund(program)` - преобразование в формат фонда
3. Интегрировать в `lib/actions/funds.ts` с приоритетом:
   - Priority 1: Fondinsan API
   - Priority 2: Zakat API
   - Priority 3: Bot API
   - Priority 4: Supabase fallback
4. Добавить переменные окружения в `.env.local`
5. Протестировать интеграцию
6. Обновить документацию

