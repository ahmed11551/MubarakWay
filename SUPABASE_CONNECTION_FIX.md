# 🔧 Исправление ошибки подключения к Supabase

## Проблема

Ошибка: **"Error: Failed to fetch (api.supabase.com)"**

Это означает, что Supabase SQL Editor не может подключиться к API.

## Решения

### 1. Проверьте подключение к интернету

- Убедитесь, что у вас есть стабильное интернет-соединение
- Попробуйте обновить страницу (F5 или Ctrl+R)

### 2. Попробуйте кнопку "Retry"

В панели результатов есть кнопка **"Retry"** - нажмите её для повторной попытки выполнения запроса.

### 3. Проверьте статус Supabase

- Откройте https://status.supabase.com
- Убедитесь, что все сервисы работают нормально

### 4. Очистите кеш браузера

- Нажмите Ctrl+Shift+Delete
- Очистите кеш и cookies
- Перезагрузите страницу

### 5. Попробуйте другой браузер

- Если используете Chrome, попробуйте Firefox или Edge
- Или наоборот

### 6. Выполните скрипт по частям

Если весь скрипт не работает, выполните его по частям:

#### Часть 1: Создать фонд Инсан

```sql
INSERT INTO public.funds (
  id,
  name,
  name_ar,
  description,
  description_ar,
  logo_url,
  category,
  is_verified,
  is_active,
  total_raised,
  donor_count,
  website_url,
  contact_email,
  created_at,
  updated_at
) VALUES (
  '00000000-0000-0000-0000-000000000001'::uuid,
  'Фонд Инсан',
  'صندوق إنسان',
  'Благотворительный фонд "Инсан" - основной партнер платформы MubarakWay. Фонд занимается различными направлениями благотворительности: помощь сиротам, образование, здравоохранение, экстренная помощь, водоснабжение и другие важные социальные программы.',
  'صندوق إنسان الخيري - الشريك الرئيسي لمنصة MubarakWay. يعمل الصندوق في مختلف مجالات الخير: مساعدة الأيتام، التعليم، الرعاية الصحية، المساعدة الطارئة، إمدادات المياه وغيرها من البرامج الاجتماعية المهمة.',
  'https://fondinsan.ru/uploads/cache/Programs/Program47/1bc0623de3-2_400x400.png',
  'general',
  true,
  true,
  0,
  0,
  'https://fondinsan.ru',
  'info@fondinsan.ru',
  NOW(),
  NOW()
)
ON CONFLICT (id) DO UPDATE SET
  name = EXCLUDED.name,
  name_ar = EXCLUDED.name_ar,
  description = EXCLUDED.description,
  description_ar = EXCLUDED.description_ar,
  logo_url = EXCLUDED.logo_url,
  category = EXCLUDED.category,
  is_verified = EXCLUDED.is_verified,
  is_active = true,
  website_url = EXCLUDED.website_url,
  contact_email = EXCLUDED.contact_email,
  updated_at = NOW();
```

#### Часть 2: Деактивировать остальные фонды

```sql
UPDATE public.funds
SET is_active = false,
    updated_at = NOW()
WHERE id != '00000000-0000-0000-0000-000000000001'::uuid
  AND is_active = true;
```

#### Часть 3: Проверить результат

```sql
SELECT id, name, is_active, category 
FROM public.funds 
WHERE is_active = true;
```

### 7. Используйте Table Editor

Если SQL Editor не работает, можно использовать Table Editor:

1. Перейдите в **Table Editor** → **funds**
2. Найдите все фонды кроме "Инсан"
3. Для каждого фонда установите `is_active = false`
4. Убедитесь, что фонд "Инсан" с ID `00000000-0000-0000-0000-000000000001` существует и `is_active = true`

### 8. Проверьте права доступа

- Убедитесь, что вы вошли в правильный аккаунт
- Проверьте, что у вас есть права на редактирование таблицы `funds`

## После исправления

После успешного выполнения скрипта:

1. ✅ Фонд "Инсан" создан и активен
2. ✅ Все остальные фонды деактивированы
3. ✅ На странице `/funds` будет отображаться только фонд "Инсан"

## Проверка

Выполните проверочный запрос:

```sql
SELECT id, name, is_active, category 
FROM public.funds 
WHERE is_active = true;
```

**Ожидаемый результат:** Одна строка с фондом "Фонд Инсан".

