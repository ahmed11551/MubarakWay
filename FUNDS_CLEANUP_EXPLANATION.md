# 🧹 Очистка фондов в базе данных

## Проблема

В базе данных находятся фонды, которых там быть не должно:
- "Помощь Хифз-центру" (Education)
- "Садака Джария" (General)  
- "Закят" (Zakat)
- И другие тестовые/старые фонды

## Откуда они взялись?

1. **Старая логика** - программы из Fondinsan API преобразовывались в отдельные фонды
2. **Тестовые данные** - скрипт `scripts/008_seed_initial_data.sql` создавал тестовые фонды
3. **Ручное добавление** - возможно, были добавлены вручную в Supabase

## Решение

### Вариант 1: Деактивация (Рекомендуется)

Скрипт `scripts/011_cleanup_funds.sql`:
- ✅ Создает/обновляет фонд "Инсан"
- ✅ Деактивирует все остальные фонды (`is_active = false`)
- ✅ Сохраняет данные (можно восстановить при необходимости)

**Преимущества:**
- Данные не теряются
- Можно восстановить при необходимости
- Безопасно для production

### Вариант 2: Полное удаление

Если вы уверены, что другие фонды не нужны:
- Раскомментируйте секцию `DELETE` в скрипте
- ⚠️ **ВНИМАНИЕ**: Это удалит фонды навсегда!

## Как применить

### Шаг 1: Откройте Supabase SQL Editor

1. Зайдите в https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в "SQL Editor"

### Шаг 2: Выполните скрипт

Скопируйте и выполните содержимое файла:
```
scripts/011_cleanup_funds.sql
```

### Шаг 3: Проверьте результат

Выполните запрос для проверки:
```sql
SELECT id, name, is_active 
FROM public.funds 
WHERE is_active = true;
```

Должен вернуться только один фонд: **"Фонд Инсан"**

## Что делать с программами Fondinsan?

Программы из Fondinsan API (например, "Помощь Хифз-центру") должны быть:
- **Кампаниями**, а не фондами
- Привязаны к фонду "Инсан" через поле `campaigns.fund_id`

Если нужно создать кампании из программ Fondinsan:
1. Получить программы через `lib/fondinsan-api.ts`
2. Создать кампании через `createCampaign()`
3. Привязать к фонду Инсан через `fund_id`

## После очистки

После выполнения скрипта:
- ✅ На странице `/funds` будет отображаться только фонд "Инсан"
- ✅ Все кампании должны быть привязаны к фонду Инсан
- ✅ Статистика будет корректной

## Откат изменений (если нужно)

Если нужно восстановить деактивированные фонды:
```sql
UPDATE public.funds
SET is_active = true
WHERE id IN (
  -- список ID фондов для восстановления
);
```

