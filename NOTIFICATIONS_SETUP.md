# 📧 Настройка уведомлений (Email/Telegram)

## ✅ Что уже реализовано

- ✅ Структура уведомлений (`lib/notifications.ts`)
- ✅ Email уведомления через Resend (`lib/email.ts`)
- ✅ Telegram уведомления (`lib/telegram.ts`)
- ✅ Автоматические уведомления при пожертвованиях
- ✅ Уведомления о статусе кампаний

## 🔧 Что нужно настроить

### 1. Email уведомления (Resend)

#### Шаг 1: Получить API ключ Resend

1. Зарегистрируйтесь на https://resend.com
2. Перейдите в **API Keys**
3. Создайте новый API ключ
4. Скопируйте ключ (начинается с `re_...`)

#### Шаг 2: Настроить домен (опционально)

1. В Resend Dashboard перейдите в **Domains**
2. Добавьте ваш домен (например, `mubarakway.app`)
3. Добавьте DNS записи для верификации
4. Дождитесь верификации

#### Шаг 3: Добавить переменные окружения

В Vercel Dashboard → Settings → Environment Variables:

| Variable | Value | Description |
|----------|-------|-------------|
| `RESEND_API_KEY` | `re_...` | API ключ от Resend |
| `RESEND_FROM_EMAIL` | `noreply@mubarakway.app` | Email отправителя (опционально) |

#### Шаг 4: Установить пакет (если не установлен)

```bash
pnpm add resend
```

---

### 2. Telegram уведомления

#### Шаг 1: Получить Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### Шаг 2: Добавить переменные окружения

В Vercel Dashboard → Settings → Environment Variables:

| Variable | Value | Description |
|----------|-------|-------------|
| `TELEGRAM_BOT_TOKEN` | `123456789:ABC...` | Токен бота от BotFather |

#### Шаг 3: Получить Telegram ID пользователей

Для отправки уведомлений пользователям нужно их `telegram_id`. Это можно сделать:

1. Через Telegram Mini App (автоматически получается из `window.Telegram.WebApp.initDataUnsafe.user.id`)
2. Через отдельного бота, который получает ID при команде `/start`
3. Вручную через админ-панель

---

## 📋 Проверка работы

### Проверка Email

После настройки переменных окружения, уведомления будут отправляться автоматически при:
- Создании пожертвования
- Одобрении/отклонении кампании
- Получении пожертвования в кампанию

### Проверка Telegram

Telegram уведомления будут отправляться автоматически, если:
- `TELEGRAM_BOT_TOKEN` настроен
- У пользователя есть `telegram_id` в профиле

---

## 🔍 Отладка

### Логи Email

Проверьте логи в Vercel Dashboard → Functions → Logs. Ищите:
- `[Email] Sent successfully` - успешная отправка
- `[Email] RESEND_API_KEY not configured` - не настроен ключ
- `[Email] Send error` - ошибка отправки

### Логи Telegram

Проверьте логи в Vercel Dashboard → Functions → Logs. Ищите:
- `[Telegram] Message sent successfully` - успешная отправка
- `[Telegram] TELEGRAM_BOT_TOKEN not configured` - не настроен токен
- `[Telegram] Send error` - ошибка отправки

---

## ⚠️ Важные замечания

1. **Email уведомления** работают только если:
   - Установлен пакет `resend`
   - Настроен `RESEND_API_KEY`
   - Домен верифицирован (для production)

2. **Telegram уведомления** работают только если:
   - Настроен `TELEGRAM_BOT_TOKEN`
   - У пользователя есть `telegram_id` в профиле

3. **Безопасность**: Никогда не коммитьте API ключи в репозиторий. Используйте только переменные окружения.

---

## 📝 Примеры использования

### Ручная отправка уведомления

```typescript
import { sendEmail } from "@/lib/email"
import { sendTelegramMessage } from "@/lib/telegram"

// Email
await sendEmail({
  to: "user@example.com",
  subject: "Тестовое уведомление",
  html: "<p>Это тестовое сообщение</p>",
})

// Telegram
await sendTelegramMessage(123456789, "Тестовое уведомление")
```

---

**Статус**: ✅ Готово к настройке  
**Приоритет**: 🟡 Важно для UX

