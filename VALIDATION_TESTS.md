# 🧪 Тесты валидации данных

Этот документ описывает все проверки валидации данных в приложении и примеры тестирования.

## 📋 Обзор валидации

Валидация данных выполняется на трех уровнях:
1. **Клиентская валидация** (в компонентах React)
2. **Server Actions валидация** (в `lib/actions/`)
3. **API валидация** (в webhook и API endpoints)

## ✅ Валидация пожертвований

### Параметры валидации

- **Минимальная сумма**: 1 (любая валюта)
- **Максимальная сумма**: 10,000,000 (любая валюта)
- **Допустимые валюты**: RUB, USD, EUR
- **Допустимые категории**: sadaqah, zakat, general
- **Допустимые типы**: one_time, recurring
- **Допустимые частоты** (для recurring): daily, weekly, monthly, yearly
- **Максимальная длина сообщения**: 500 символов
- **Формат ID**: UUID (для fundId и campaignId)

### Тесты валидации суммы

#### ✅ Валидные суммы
```javascript
// Минимальная сумма
amount: 1

// Обычная сумма
amount: 1000

// Максимальная сумма
amount: 10000000

// С дробной частью
amount: 100.50
```

#### ❌ Невалидные суммы
```javascript
// Отрицательная сумма
amount: -100
// Ожидаемая ошибка: "Сумма должна быть больше 0"

// Ноль
amount: 0
// Ожидаемая ошибка: "Сумма должна быть больше 0"

// Превышение максимума
amount: 10000001
// Ожидаемая ошибка: "Максимальная сумма: 10 000 000"

// Не число
amount: "abc"
// Ожидаемая ошибка: "Сумма пожертвования должна быть числом"

// NaN
amount: NaN
// Ожидаемая ошибка: "Сумма пожертвования должна быть числом"
```

### Тесты валидации валюты

#### ✅ Валидные валюты
```javascript
currency: "RUB"
currency: "USD"
currency: "EUR"
```

#### ❌ Невалидные валюты
```javascript
currency: "BTC"
// Ожидаемая ошибка: "Неверная валюта. Допустимые валюты: RUB, USD, EUR"

currency: ""
// Ожидаемая ошибка: "Неверная валюта. Допустимые валюты: RUB, USD, EUR"

currency: null
// Ожидаемая ошибка: "Неверная валюта. Допустимые валюты: RUB, USD, EUR"
```

### Тесты валидации типа пожертвования

#### ✅ Валидные типы
```javascript
donationType: "one_time"
donationType: "recurring"
```

#### ❌ Невалидные типы
```javascript
donationType: "monthly"
// Ожидаемая ошибка: "Неверный тип пожертвования. Допустимые типы: one_time, recurring"

donationType: ""
// Ожидаемая ошибка: "Неверный тип пожертвования. Допустимые типы: one_time, recurring"
```

### Тесты валидации регулярных пожертвований

#### ✅ Валидные частоты
```javascript
// Для recurring типа
{
  donationType: "recurring",
  frequency: "daily"
}
{
  donationType: "recurring",
  frequency: "weekly"
}
{
  donationType: "recurring",
  frequency: "monthly"
}
{
  donationType: "recurring",
  frequency: "yearly"
}
```

#### ❌ Невалидные частоты
```javascript
// Отсутствует frequency для recurring
{
  donationType: "recurring",
  frequency: undefined
}
// Ожидаемая ошибка: "Для регулярных пожертвований необходимо указать периодичность: daily, weekly, monthly, yearly"

// Неверная частота
{
  donationType: "recurring",
  frequency: "hourly"
}
// Ожидаемая ошибка: "Для регулярных пожертвований необходимо указать периодичность: daily, weekly, monthly, yearly"
```

### Тесты валидации категории

#### ✅ Валидные категории
```javascript
category: "sadaqah"
category: "zakat"
category: "general"
```

#### ❌ Невалидные категории
```javascript
category: "charity"
// Ожидаемая ошибка: "Неверная категория. Допустимые категории: sadaqah, zakat, general"

category: ""
// Ожидаемая ошибка: "Неверная категория. Допустимые категории: sadaqah, zakat, general"
```

### Тесты валидации сообщения

#### ✅ Валидные сообщения
```javascript
message: "В память о моей матери"
message: "" // Пустое сообщение допустимо
message: undefined // undefined допустимо
message: "A".repeat(500) // Максимальная длина
```

#### ❌ Невалидные сообщения
```javascript
message: "A".repeat(501)
// Ожидаемая ошибка: "Сообщение не должно превышать 500 символов"
```

### Тесты валидации ID

#### ✅ Валидные ID (UUID формат)
```javascript
fundId: "123e4567-e89b-12d3-a456-426614174000"
campaignId: "123e4567-e89b-12d3-a456-426614174000"
```

#### ❌ Невалидные ID
```javascript
fundId: "123"
// Ожидаемая ошибка: "Неверный формат ID фонда"

campaignId: "not-a-uuid"
// Ожидаемая ошибка: "Неверный формат ID кампании"

fundId: "123e4567-e89b-12d3-a456"
// Ожидаемая ошибка: "Неверный формат ID фонда"
```

## ✅ Валидация платежей

### Тесты валидации URL

#### ✅ Валидные URL
```javascript
returnUrl: "https://example.com/success"
returnUrl: "http://localhost:3000/success"
cancelUrl: "https://example.com/cancel"
```

#### ❌ Невалидные URL
```javascript
returnUrl: "javascript:alert('xss')"
// Ожидаемая ошибка: "Return URL должен использовать протокол HTTP или HTTPS"

returnUrl: "not-a-url"
// Ожидаемая ошибка: "Неверный формат Return URL"

cancelUrl: "ftp://example.com"
// Ожидаемая ошибка: "Cancel URL должен использовать протокол HTTP или HTTPS"
```

## ✅ Валидация Webhook

### Тесты валидации подписи

#### ✅ Валидная подпись
```javascript
// Правильная HMAC SHA-256 подпись
signature: crypto.createHmac("sha256", secret).update(body).digest("base64")
```

#### ❌ Невалидная подпись
```javascript
signature: "wrong-signature"
// Ожидаемый ответ: 401 "Invalid signature"

signature: ""
// Ожидаемый ответ: 401 "Invalid signature" (если secret настроен)
```

### Тесты валидации тела запроса

#### ✅ Валидное тело
```json
{
  "Model": {
    "TransactionId": "123456",
    "InvoiceId": "{\"donationId\":\"uuid\"}",
    "Status": "Completed"
  }
}
```

#### ❌ Невалидное тело
```javascript
// Отсутствует Model
{}
// Ожидаемый ответ: 400 "Missing Model field"

// Отсутствует TransactionId
{
  "Model": {
    "InvoiceId": "123",
    "Status": "Completed"
  }
}
// Ожидаемый ответ: 400 "Missing TransactionId"

// Отсутствует InvoiceId
{
  "Model": {
    "TransactionId": "123",
    "Status": "Completed"
  }
}
// Ожидаемый ответ: 400 "Missing InvoiceId"

// Невалидный JSON
"{ invalid json }"
// Ожидаемый ответ: 400 "Invalid JSON body"
```

## 🧪 Примеры тестирования

### Тест 1: Валидация минимальной суммы
```typescript
const result = await createDonation({
  amount: 0.5,
  currency: "RUB",
  donationType: "one_time",
  category: "sadaqah",
  isAnonymous: false
})
// Ожидается: { error: "Минимальная сумма пожертвования: 1 RUB" }
```

### Тест 2: Валидация максимальной суммы
```typescript
const result = await createDonation({
  amount: 10000001,
  currency: "RUB",
  donationType: "one_time",
  category: "sadaqah",
  isAnonymous: false
})
// Ожидается: { error: "Максимальная сумма пожертвования: 10 000 000 RUB" }
```

### Тест 3: Валидация регулярного пожертвования без частоты
```typescript
const result = await createDonation({
  amount: 1000,
  currency: "RUB",
  donationType: "recurring",
  category: "sadaqah",
  isAnonymous: false
  // frequency отсутствует
})
// Ожидается: { error: "Для регулярных пожертвований необходимо указать периодичность: daily, weekly, monthly, yearly" }
```

### Тест 4: Валидация неверного формата ID
```typescript
const result = await createDonation({
  amount: 1000,
  currency: "RUB",
  donationType: "one_time",
  category: "sadaqah",
  fundId: "invalid-id",
  isAnonymous: false
})
// Ожидается: { error: "Неверный формат ID фонда" }
```

### Тест 5: Валидация длинного сообщения
```typescript
const result = await createDonation({
  amount: 1000,
  currency: "RUB",
  donationType: "one_time",
  category: "sadaqah",
  message: "A".repeat(501),
  isAnonymous: false
})
// Ожидается: { error: "Сообщение не должно превышать 500 символов" }
```

## 🔒 Безопасность

### Защита от SQL Injection
- Все запросы к БД выполняются через Supabase Client, который автоматически экранирует параметры
- Прямые SQL запросы не используются

### Защита от XSS
- Все пользовательские данные экранируются при выводе
- React автоматически экранирует значения в JSX

### Защита от CSRF
- Webhook проверяет подпись HMAC для подтверждения источника запроса
- Server Actions требуют аутентификации пользователя

## 📝 Примечания

- Валидация выполняется как на клиенте, так и на сервере
- Серверная валидация является основной защитой
- Клиентская валидация улучшает UX, но не заменяет серверную
- Все ошибки валидации возвращаются на русском языке для лучшего UX

