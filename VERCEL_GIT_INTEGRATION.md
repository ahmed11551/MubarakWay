# 🚀 Настройка встроенной интеграции Vercel с GitHub

## ✅ Преимущества

- Автоматический деплой при каждом push в `main`
- Не нужно настраивать GitHub Actions
- Автоматическая сборка и деплой
- Проще управление переменными окружения

---

## 📋 Шаги настройки

### 1. Подключите репозиторий в Vercel

1. Откройте **Vercel Dashboard**: https://vercel.com/dashboard
2. Нажмите **"Add New..."** → **"Project"**
3. Выберите **"Import Git Repository"**
4. Найдите репозиторий `ahmed11551/SadakaPass`
5. Нажмите **"Import"**

### 2. Настройте проект

**Framework Preset:** Next.js (должен определиться автоматически)

**Root Directory:** `.` (корень проекта)

**Build Command:** `pnpm build` (или оставьте по умолчанию)

**Output Directory:** `.next` (или оставьте по умолчанию)

**Install Command:** `pnpm install`

### 3. Добавьте переменные окружения

В настройках проекта перейдите в **Settings** → **Environment Variables** и добавьте:

| Variable Name | Value | Environment |
|--------------|-------|-------------|
| `NEXT_PUBLIC_SUPABASE_URL` | Ваш Supabase URL | Production, Preview, Development |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Ваш Supabase Anon Key | Production, Preview, Development |
| `SUPABASE_SERVICE_ROLE_KEY` | Ваш Supabase Service Role Key | Production, Preview, Development |
| `API_AUTH_TOKEN` | `test_token_123` | Production, Preview, Development |
| `TELEGRAM_BOT_TOKEN` | Ваш Telegram Bot Token | Production, Preview, Development |
| `TELEGRAM_SECRET_TOKEN` | Ваш Telegram Secret Token | Production, Preview, Development |
| `BOT_API_BASE_URL` | `https://bot.e-replika.ru` | Production, Preview, Development |
| `BOT_API_TOKEN` | `test_token_123` | Production, Preview, Development |

### 4. Деплой

После настройки:
1. Нажмите **"Deploy"**
2. Vercel автоматически:
   - Установит зависимости (`pnpm install`)
   - Соберёт проект (`pnpm build`)
   - Задеплоит на production

### 5. Автоматические деплои

После первого деплоя:
- ✅ Каждый push в `main` → автоматический деплой на production
- ✅ Pull Request → автоматический preview деплой
- ✅ Все переменные окружения применяются автоматически

---

## 🔄 Отключение GitHub Actions (опционально)

GitHub Actions workflow можно оставить как резервный вариант или отключить:

**Вариант 1: Отключить workflow**
- Переименуйте `.github/workflows/vercel-deploy.yml` в `.github/workflows/vercel-deploy.yml.disabled`

**Вариант 2: Оставить как резерв**
- Оставьте как есть - он не будет мешать, если Vercel Git Integration активна

---

## ✅ Проверка

После настройки:

1. **Проверьте деплой:**
   - Откройте Vercel Dashboard → ваш проект
   - Должен быть успешный деплой

2. **Проверьте сайт:**
   - Откройте ваш Vercel URL (например: `https://sadaka-pass.vercel.app`)
   - Блок быстрого пожертвования должен быть виден

3. **Проверьте автоматический деплой:**
   - Сделайте небольшое изменение в коде
   - Сделайте commit и push
   - В Vercel Dashboard должен появиться новый деплой автоматически

---

## 🎯 Преимущества этого подхода

- ✅ Не нужно настраивать GitHub Secrets
- ✅ Не нужно управлять workflow файлами
- ✅ Автоматические preview деплои для PR
- ✅ Проще откатывать деплои
- ✅ Встроенный мониторинг и логи

---

**Готово!** Теперь каждый push в `main` будет автоматически деплоиться на Vercel.

