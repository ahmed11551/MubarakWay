# 🔧 Исправление проблемы со сворачиванием Telegram WebApp при скролле

## Проблема

При скроллинге контента вверх внутри мини-приложения Telegram происходит автоматическое сворачивание (сдвиг вниз) WebApp. Это происходит на iOS и Android в актуальных версиях Telegram.

## Решение

Реализовано комплексное решение для предотвращения сворачивания при скролле:

### 1. CSS Fixes (`app/globals.css`)

Добавлены стили для предотвращения pull-to-close поведения:

```css
html.telegram-webapp {
  overscroll-behavior: none;
  overscroll-behavior-y: contain;
  -webkit-overflow-scrolling: touch;
  height: 100%;
  overflow: hidden;
}

html.telegram-webapp body {
  overscroll-behavior: none;
  overscroll-behavior-y: contain;
  touch-action: pan-y;
}

html.telegram-webapp * {
  overscroll-behavior-y: contain;
}
```

### 2. JavaScript Touch Event Handling (`lib/telegram.ts`)

Реализована функция `setupScrollPrevention()` которая:

- **Определяет scrollable контейнер** - находит ближайший прокручиваемый родительский элемент
- **Отслеживает границы скролла** - проверяет, находится ли пользователь в начале или конце контента
- **Предотвращает pull-to-close** - блокирует жесты закрытия при скролле внутри контента
- **Обрабатывает touch события** - использует capture phase для раннего перехвата событий

### 3. Viewport Management

- Использует `viewportStableHeight` для правильной высоты
- Слушает события `viewportChanged` для обновления при изменении
- Устанавливает минимальную высоту для предотвращения layout shifts

## Как это работает

1. **При touchstart**: Определяется scrollable контейнер и начальная позиция касания
2. **При touchmove**: 
   - Проверяется, является ли жест вертикальным скроллом
   - Определяется позиция скролла (верх, низ, середина)
   - **Если контент короче viewport** - блокируются все pull-to-close жесты
   - **Если пользователь в начале контента** и пытается скроллить вверх (pull down) - предотвращается закрытие
   - **Если пользователь в конце контента** и пытается скроллить вниз (pull up) - блокируется закрытие
   - **Если пользователь в середине контента** - всегда блокируется передача события WebApp (самый важный случай)
3. **При touchend**: Сбрасывается состояние

## Тестирование

### Проверка 1: Вертикальный скролл
- ✅ Скролл вверх должен работать плавно
- ✅ Скролл вниз должен работать плавно
- ✅ WebApp не должен сворачиваться при скролле

### Проверка 2: Быстрые свайпы
- ✅ Быстрый свайп вверх не должен закрывать приложение
- ✅ Быстрый свайп вниз не должен закрывать приложение
- ✅ Скролл должен оставаться плавным

### Проверка 3: Pull-to-close
- ✅ Pull-to-close должен работать только когда контент в самом верху (и только при явном жесте вниз)
- ✅ При скролле внутри контента pull-to-close должен быть полностью заблокирован
- ✅ При коротком контенте (меньше viewport) pull-to-close должен быть заблокирован
- ✅ При скролле вниз от нижней границы контента закрытие должно быть заблокировано
- ✅ Закрытие через `enableClosingConfirmation()` должно работать

## Платформы

- ✅ iOS (Safari в Telegram)
- ✅ Android (Chrome в Telegram)
- ✅ Desktop (если открыто в браузере)

## Дополнительные настройки

### Если проблема сохраняется:

1. **Проверьте версию Telegram WebApp**:
   ```javascript
   console.log(window.Telegram.WebApp.version)
   ```

2. **Убедитесь, что WebApp расширен**:
   ```javascript
   window.Telegram.WebApp.expand()
   ```

3. **Проверьте viewport height**:
   ```javascript
   console.log(window.Telegram.WebApp.viewportStableHeight)
   ```

4. **Добавьте дополнительные логи**:
   ```javascript
   // В handleTouchMove добавьте:
   console.log('Scroll:', { isAtTop, isAtBottom, deltaY, scrollTop })
   ```

## Известные ограничения

- На очень старых версиях Telegram может не работать полностью
- На некоторых Android устройствах может потребоваться дополнительная настройка
- ~~Если контент очень короткий (меньше viewport), pull-to-close может работать всегда~~ ✅ **ИСПРАВЛЕНО**: Теперь короткий контент защищен от pull-to-close

## Альтернативные решения

Если текущее решение не работает, можно попробовать:

1. **Использовать библиотеку** `@twa-dev/sdk` для более продвинутого управления
2. **Настроить через Bot API** параметры WebApp при создании бота
3. **Использовать iframe** для изоляции скролла (не рекомендуется)

## Отладка

Для отладки добавьте в консоль:

```javascript
// В setupScrollPrevention добавьте:
window.__telegramScrollDebug = {
  touchStartY,
  touchStartX,
  scrollContainerElement,
  isVerticalScroll,
  getScrollInfo: () => {
    const container = scrollContainerElement || document.body
    return {
      scrollTop: container.scrollTop || window.pageYOffset,
      scrollHeight: container.scrollHeight || document.documentElement.scrollHeight,
      clientHeight: container.clientHeight || window.innerHeight,
    }
  }
}
```

Затем в консоли можно проверить:
```javascript
window.__telegramScrollDebug.getScrollInfo()
```

