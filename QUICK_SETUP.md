# 🚀 Быстрая настройка Vercel

## Автоматическая настройка (1 команда)

### Windows (PowerShell):
```powershell
.\scripts\setup-vercel-env.ps1
```

### Linux/Mac:
```bash
node scripts/setup-vercel-env.js
```

## Что нужно подготовить:

1. **Supabase URL и ключи:**
   - Откройте https://supabase.com/dashboard
   - Выберите проект → Settings → API
   - Скопируйте:
     - Project URL
     - anon public key
     - service_role key

2. **Авторизация в Vercel:**
   ```bash
   vercel login
   ```

## После запуска скрипта:

Скрипт попросит ввести значения и автоматически добавит их в Vercel для всех окружений (production, preview, development).

Vercel автоматически пересоберет проект после добавления переменных.

## Альтернатива: Ручная настройка

Если скрипт не работает, используйте веб-интерфейс:

1. Откройте: https://vercel.com/dashboard
2. Выберите проект `mubarak-way`
3. Settings → Environment Variables
4. Добавьте переменные (см. VERCEL_SETUP.md)

