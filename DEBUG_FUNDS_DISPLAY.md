# 🔍 Отладка проблемы с отображением фонда

## ✅ Что проверено

1. **Фонд в базе данных**: ✅ Есть
   - ID: `00000000-0000-0000-0000-000000000001`
   - Название: "Фонд Инсан"
   - Статус: `is_active = true`
   - Категория: `general`

2. **RLS политики**: ✅ Настроены правильно
   - Политика `funds_select_all` разрешает SELECT для активных фондов
   - Условие: `is_active = true`

3. **Код**: ✅ Обновлен с логированием

---

## 🔧 Как проверить проблему

### Вариант 1: Проверить логи Vercel

1. Откройте Vercel Dashboard
2. Перейдите в **Deployments** → выберите последний деплой
3. Откройте **Function Logs**
4. Найдите логи с префиксом `[v0]` или `[FundsPage]`
5. Проверьте, что возвращает запрос

### Вариант 2: Использовать debug endpoint

После деплоя откройте в браузере:
```
https://ваш-домен.vercel.app/api/funds?debug=true
```

Это покажет:
- Результат прямого запроса к Supabase
- Результат функции `getFunds()`
- Любые ошибки

### Вариант 3: Проверить в браузере

1. Откройте страницу `/funds`
2. Откройте DevTools (F12)
3. Перейдите в **Console**
4. Найдите логи с префиксом `[FundsPage]` или `[v0]`
5. Проверьте, что возвращается

---

## 🐛 Возможные причины

### 1. Проблема с RLS политиками
**Симптом**: Запрос возвращает пустой массив без ошибок

**Решение**: Проверьте, что политика работает для анонимных пользователей:
```sql
-- Проверить политики
SELECT * FROM pg_policies WHERE tablename = 'funds';

-- Если нужно, пересоздать политику
DROP POLICY IF EXISTS "funds_select_all" ON public.funds;
CREATE POLICY "funds_select_all"
  ON public.funds FOR SELECT
  USING (is_active = true);
```

### 2. Проблема с переменными окружения
**Симптом**: Ошибка "Missing Supabase environment variables"

**Решение**: Проверьте в Vercel:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 3. Проблема с кэшированием
**Симптом**: Старые данные отображаются

**Решение**: 
- Очистите кэш браузера
- Перезапустите деплой на Vercel

### 4. Проблема с cookies/сессией
**Симптом**: Ошибка при создании клиента Supabase

**Решение**: Проверьте, что `createClient()` работает корректно

---

## 📝 Что делать дальше

1. **Проверьте логи Vercel** после следующего деплоя
2. **Используйте debug endpoint** для диагностики
3. **Проверьте консоль браузера** на наличие ошибок
4. **Сообщите результаты** - какие логи вы видите?

---

## 🔄 Быстрое решение

Если нужно быстро показать фонд, можно временно использовать прямой запрос:

```typescript
// В app/funds/page.tsx временно
const supabase = await createClient()
const { data: funds } = await supabase
  .from("funds")
  .select("*")
  .eq("is_active", true)
```

Но лучше сначала найти причину через логи.

