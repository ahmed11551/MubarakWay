# 🔧 Исправление ошибок в Vercel

## Проблема

Коэффициент ошибок: **8.4%** в production на Vercel.

## Возможные причины

### 1. Отсутствие поля `fund_id` в таблице `campaigns`

**Проблема:**
- В коде используется `campaigns.fund_id`
- Если миграция не запущена, поле не существует
- Это вызывает ошибки при запросах

**Решение:**
- ✅ Добавлена обработка ошибок в `app/funds/[id]/page.tsx`
- ✅ Запросы к campaigns теперь обрабатывают отсутствие поля gracefully
- ✅ Нужно запустить миграцию `scripts/010_create_insan_fund.sql`

### 2. Отсутствие фонда "Инсан" в БД

**Проблема:**
- Код ожидает фонд с UUID `00000000-0000-0000-0000-000000000001`
- Если фонд не создан, запросы могут возвращать пустые результаты

**Решение:**
- ✅ Добавлены предупреждения в логах
- ✅ Код возвращает пустой массив вместо ошибки
- ✅ Нужно запустить миграцию `scripts/010_create_insan_fund.sql`

### 3. Отсутствие переменных окружения Supabase

**Проблема:**
- Если `NEXT_PUBLIC_SUPABASE_URL` или `NEXT_PUBLIC_SUPABASE_ANON_KEY` не установлены
- `createClient()` выбрасывает ошибку

**Решение:**
- ✅ Ошибка обрабатывается в `lib/supabase/server.ts`
- ✅ Нужно проверить переменные окружения в Vercel

### 4. Синтаксис Supabase `.or()`

**Проблема:**
- Строка 21 в `lib/actions/funds.ts` использует `.or()`
- Может быть неправильный синтаксис для некоторых версий Supabase

**Решение:**
- ✅ Добавлены комментарии
- ⚠️ Может потребоваться проверка синтаксиса

## Что нужно сделать

### 1. Запустить миграцию в Supabase

```sql
-- Выполнить в Supabase SQL Editor
-- Файл: scripts/010_create_insan_fund.sql
```

Это создаст:
- Фонд "Инсан"
- Поле `fund_id` в таблице `campaigns` (если его нет)
- Индекс для производительности

### 2. Проверить переменные окружения в Vercel

Убедитесь, что установлены:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY` (если используется)

### 3. Проверить логи Vercel

В Vercel Dashboard:
1. Откройте проект `mubarak-way`
2. Перейдите в "Functions" → "Logs"
3. Найдите ошибки за последние 6 часов
4. Проверьте stack trace

## Исправления в коде

### ✅ Добавлена обработка ошибок

1. **app/funds/[id]/page.tsx:**
   - Обработка отсутствия поля `fund_id`
   - Graceful fallback для запросов к campaigns
   - Обработка ошибок статистики

2. **lib/actions/funds.ts:**
   - Убраны неиспользуемые импорты
   - Улучшена обработка ошибок
   - Добавлены предупреждения в логах

3. **app/api/funds/route.ts:**
   - Обновлен комментарий
   - Улучшена обработка ошибок

## Мониторинг

После применения исправлений:

1. Проверьте логи Vercel через 10-15 минут
2. Убедитесь, что коэффициент ошибок снизился
3. Проверьте работу страниц:
   - `/funds` - должен показывать фонд Инсан
   - `/funds/{id}` - должен показывать отчетность
   - `/campaigns/new` - должен позволять выбрать фонд

## Дополнительные проверки

Если ошибки продолжаются:

1. **Проверьте RLS политики в Supabase:**
   - Убедитесь, что политики для `funds` и `campaigns` настроены правильно

2. **Проверьте индексы:**
   - Убедитесь, что индексы созданы для `fund_id`

3. **Проверьте версию Supabase клиента:**
   - Убедитесь, что используется актуальная версия `@supabase/ssr`

